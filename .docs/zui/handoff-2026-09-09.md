# ZUI 阶段交接与周预算边界

## 状态与恢复入口

- 主入口仍是 [生产执行计划](./production-execution-plan-2026-09-08.md)，本交接不缩减 G0–G4 或宣称生产验收完成。
- 用户要求主 Codex **周额度剩余 30% 时停止**。剩余约 33% 后不再开启新批次或扩展子代理，只做必要收口；不得用 Spark 的独立额度绕过停止线。
- 2026-09-09 03:38（北京时间）实时查询：主周额度剩余 34%，预计 2026-09-15 09:25:04 重置。本次查询尚未到停止线，这不是“已因预算暂停”的记录；后续必须重新查询，不能依据此静态数字恢复或继续开发。
- 04:10 再查剩余 33%，进入只收尾阶段：只完成已复现的 P16 DataTable 修复、验证与提交，不开启新组件族或增加子代理。仍未到 30% 硬停止线，不把此记录冒充已因预算暂停。
- 既有恢复检查 `zui` 每周二 09:30 检查实际重置。只有本任务明确因预算暂停、额度恢复且用户没有后续停止指令时才续作；需要本机与应用运行，不能声称能自行切换用户控制的目标模式开关。
- 恢复时先读取实际 HEAD、工作区和精确 SHA 的 CI。其他会话修改不能覆盖，日志过期不能冒充当前结果；npm 发布、tag 和生产部署仍需独立授权。

## 当前批次 P16

- 提交为 `f0e71706e5eda473682c216834c3c777108eba84`，与前一条交接提交 `40cbaef` 已一起推送；当前工作树应以实际 Git 检查为准。
- 从 P15 coverage 未执行的 `beginResize` 路径复现缺陷：无关 pointer 将 LTR 宽度 160 改成 320、RTL 压到 64。移动、up、cancel 现在按发起 pointerId 过滤；结束 listener 不使用会被无关触点消耗的 once，真正结束或卸载时集中释放。
- 实例公开 `dir="rtl"` 只影响 DataTable 外层、内部 ZTable 回落到默认 LTR，已通过传入真实 prop 的测试复现。现在外层与 ZTable 共享 `dir ?? Provider.direction`，pointer 与 keyboard 均读取分隔线实际方向；没有新增平行方向 API。
- 14 项 Chromium 定向用例已通过：原有 8 项、4 项指针事件归属、2 项真实鼠标路径。真实鼠标检查先滚入视口并验证实际命中目标；多 pointer 回归是浏览器内事件派发，不冒称真机双指/CDP触屏验收。
- 初始失败保留在 `.codex/p16-datatable-pointer-before.log`、`p16-datatable-mouse-hit.log`、`p16-datatable-direction-before.log`。冻结后的完整收尾结果以 `.codex/p16-datatable-recheck-final.json` 为准，未结束项不算通过。Svelte MCP 未在本任务提供，使用实际 Svelte compiler / 类型检查与浏览器验证。
- P16 本地收尾已结束：14项Chromium、ZUI/Docs类型0 errors/0 warnings、生成/token同步、lifecycle、lint/format和audit:system全部通过。其他浏览器、完整coverage和Docs全站等待本批新SHA的CI，不外推P15结果。
- [P16 CI 34273892722](https://github.com/kenconnet666/zadmin/actions/runs/34273892722) 已全部结束，最终failure，唯一独立失败项是Coverage门槛（Required aggregate随之失败）。三浏览器组件、Static、build、外部包、Windows、Drizzle与证据合并通过，三浏览器Docs各227项通过。Chromium主套件完整通过323文件2133项，其中DataTable14项通过，独立触屏17项通过；这是P15之后连续第二轮未断连，仍非历史根因已证明解决。Coverage同样323文件2133项全过，但global未覆盖lines/functions/statements/branches为1560/456/2921/3734、components为1290/374/2432/3094，仍超原有门槛。以上只验收P16代码SHA，不证明完整能力目标已实现。
- P15的WebKit DatePicker失败用例在P16原样通过，期间没有DatePicker或该测试的修复；仍保留下面的trace作为待调查不稳定线索。不要把后续仅交接文档的提交另算一次功能交付，也不要为每个doc-only CI反复生成状态提交。
- P16当前家族的controller验证补齐：新增3项真实controller回归，确认resetColumnWidths写回配置默认值或清除覆盖、重复重置不重复通知、外部同步不通知、重置payload冻结且controller/row身份保持。实现已经正确复用sameStateValue，本次未修改组件源码或共享状态层。Docs明确reset目标和通知语义；此API不是原生form reset。
- 补齐后本地DataTable套件17项通过（`.codex/p16-controller-browser.log`），ZUI/Docs类型0 errors/0 warnings，生成、lifecycle、lint/format、audit:system通过（`.codex/p16-controller-recheck-final.json`）。首轮测试数据被推断为含可选undefined的record而类型失败，已用公开DataTableColumnWidths显式标注用例数组，不放宽库类型；旧日志保留。补充测试的远端结果仍需新SHA验收。
- 列可见性controller收尾发现并复现真实缺陷：合法constructor列因误读Object.prototype.constructor为宽度而在SSR抛错。新增局部columnStateValue，统一可见性/宽度/两个controller写入判断仅读取自身键；保留constructor、toString、__proto__为合法ID，不改全局状态比较。自身键覆盖、继承值忽略、同步连续隐藏的最后一列保护、未知/重复请求不通知、不可变visibleColumnIds及SSR都有回归。
- 该修复本地20项混合SSR/Chromium通过，ZUI/Docs类型0 errors/0 warnings，生成/token同步、lifecycle、lint/format和audit:system通过；见`.codex/p16-column-state-after.log`、`p16-column-state-final.json`。修前红证据见`p16-column-state-before.log`，不得把新SSR登记数量当成整库成熟度。
- 最新已结束的[CI 34278196491](https://github.com/kenconnet666/zadmin/actions/runs/34278196491)绑定前置257f3f68a6b93d95b43620919bf3b64d2d3ae4cb：DataTable17项通过，但Chromium在113个browser文件完成、下一choice-virtualization tester阶段再次断连，只有266/323文件、2010项通过；独立触屏17项通过。CDP与Playwright都记录Vite/control WebSocket错误，文本仍为空，未取得底层错误码。Coverage323文件2136项全过，global未覆盖1558/454/2920/3734、components1288/372/2431/3094仍超原门槛。不要沿用此前连续通过即“已解决”的推断。

## 前置批次 P15

P15 已提交推送为 `7180baa8c89c08a0d8617b3ef10fd547ed806363`，前置提交为 `9ddb8b5666bb726048031116dabbc36cb8943387`。下面的远端记录绑定 P15 SHA，不外推为后续代码已验收。

- `layout-motion-capture.ts` 收拢 Transfer / Sortable 的 stable-key 布局捕获和监听生命周期，保留各自 canonical 数据、请求、焦点和终态 owner。
- 观察容器与已测量行的 border box，滚动、尺寸变化、取消会使旧动画失效；初次同尺寸通知不误判。reduced / 零时长不读取历史几何。未承诺侦测所有仅 transform、gap 或祖先位置变化。
- Sortable 的业务接受与动画可用性分离：同批回写并接受有动画，已经独立呈现的提前回写不在迟到接受时倒播。Docs 合同同步，没有新增不必要公开 API。
- 可选 Chromium Network 诊断仅采集 WebSocket 脱敏路径、错误和资源类型。独立 CDP session，不干扰 Vitest / touch 使用的会话；不记录 headers、body、query、hash 或 frame payload。`network-ready` 标明真实启用；主 CI 与 coverage 对照启用 `ZUI_BROWSER_NETWORK_DIAGNOSTICS=1`。
- 诊断注册、部分失败、清理失败、异步 setup 与关闭竞态均有 unit。诊断不会通过重试、增加并发、强制 GC、修改端口或放宽超时来改变结果。

## 验证记录

- 首次生成与 token 更新通过；类型检查遇到编辑中的诊断参数和测试替身字面类型不一致，已修正，旧失败保留在 `.codex/production-p15-final.json` 与对应日志。
- 冻结后的 ZUI / Docs 类型均为 0 errors / 0 warnings；15 项 unit / SSR、26 项定向 Chromium、17 项触屏模拟、2 项 Docs、资源审计、生成、格式与 lint 均通过，见 `.codex/production-p15-recheck-final.json`。
- 真实运行中 Page 正常关闭后的重复 detach 产生诊断误报，已增加 Page 关闭状态的窄处理。最终 14 项 unit + browser 复验、ZUI 类型、格式与 lint 通过，日志含 `network-ready` 且无诊断 setup / cleanup 误报，见 `.codex/p15-diagnostics-final.json`。它不是整库断连已修复的证据。
- 本机不再运行整库三浏览器或全量 coverage，交新 SHA 的 CI 验证。CDP 输入模拟不等于物理设备验收。

## 已知未闭合项与下一步

1. P15 的 [CI 34270838463](https://github.com/kenconnet666/zadmin/actions/runs/34270838463) 中，Chromium 主套件完整通过 323 文件 / 2127 项，独立触屏 17 项通过；Firefox/WebKit 组件任务也通过。新增 Network 诊断已实际启用且本轮未采到连接错误。前置多轮断连的根因仍未证明解决，单轮成功和观察器改变后的运行不能替代稳定性与因果验证，不凭空命名错误码。
2. P15 coverage 的 323 文件 / 2127 项测试全过，但未覆盖 global lines/functions/statements/branches 为 1595/459/2960/3755、components 为 1325/377/2471/3115，仍超既有预算。完整 JSON 已从该 SHA 的 `coverage-7180baa8c89c08a0d8617b3ef10fd547ed806363` artifact 获取。本地副本在 `.codex/p15-coverage-7180`，只用于诊断；不下调门槛、不排除脚本或只增加浅 smoke。
3. P15 CI 已全部结束，最终 failure。Static、build、外部包、Windows、Drizzle、三浏览器组件、Docs Chromium/Firefox 与证据合并通过。Docs WebKit 为 226 通过 / 1 失败，`site.e2e.ts:869` 的 DatePicker 选择 20 日后 trigger 未聚焦，截图却仍是旧值 `2026-08-18` 且弹层打开；不能仅归为焦点恢复延迟。截图、error-context 与 trace 位于该 SHA 的 `docs-playwright-webkit` artifact，本地副本 `.codex/p16-docs-webkit-7180`。下一步先重建 click→Calendar提交→Picker关闭/焦点链，不放宽 timeout 或替换成键盘使该 pointer 用例绕过失败。
4. 确认 P15 CI 后，再按主计划推进未审计组件族、公共一致性、企业/领域能力及演示整理。旧的 Firefox 动画中间态失败并未稳定复现，不用放宽 timeout 代替诊断。
5. 依赖保持已经安装验证的 Playwright 1.63、Vitest 5、Node 24、TypeScript 6；后续升级重新核实官方支持与 1440 分钟严格供应链等待，不为“最新”跳过兼容和供应链规则。

### P15 WebKit trace 的进一步只读线索

`1-trace.trace` 的 `call@2012` 对应点击“2026年8月20日星期四”：先记录元素不稳定，随后两次被“27日”按钮拦截；输入坐标依次为 `(567.39,583.79)`、`(567.39,359.79)`、`(567.39,16.79)`，最后 click action 返回成功，但截图仍是18日且弹层打开。优先排查目标/祖先滚动与浮层重定位、鼠标命中检查之间的时序；这是定位线索，未证明具体组件、定位引擎或浏览器的因果责任。不得只把后续 toBeFocused 的等待放宽。

### 下一轮覆盖率与 API 验证优先序

以下来自 P15 的实际 coverage JSON，并核对当前源码和已有浏览器测试；它们是待验证路径，不直接等同已确认功能缺陷。Svelte 生成回调也会计入统计，不能把函数数量当 API 完成率。

- **DataTable**：P15 未覆盖 134 branches / 13 functions。P16 pointer/方向及resetColumnWidths等17项已通过远端；新的自身键隔离、可见性返回值/快照与最后一列保护本地20项通过，待新SHA。完整min/max、卸载中的拖动、受控owner拒绝等仍需专门验证，不能把这些小批外推为列宽API全部验收。
- **Tree**：未覆盖 127 branches / 22 functions。懒加载 owner 已有行为回归，但 controller 的 `loadingKeys` / `errorKeys` / `focusKey` 等仍未执行。补状态快照、expandAncestors、虚拟窗口内外 focus / scroll 与删除节点，不重复添加普通展开 smoke。
- **NavigationMenu**：未覆盖 124 branches / 18 functions。现有 overflow 用例证明节点身份、隐藏与 popup 布局，但未覆盖溢出项 `onNavigateRequest` 的取消权、`defaultPrevented` 和 `close()` 透传。补真正激活目标后的原生导航/回调/关闭与焦点结果，不只检查菜单能打开。
- **后续族**：DateTimeRangePicker、Splitter、TimeRangePicker 的未覆盖分支分别为 101、93、85。下一轮须先读取精确 branchMap 与当前测试，再决定补哪些真实接受/拒绝/reset、拖动/键盘或边界路径；不据这些数字一次性写模板测试。

## 操作边界

- 当前 WebStorm Docs 地址 `http://127.0.0.1:5174`，不要为测试杀掉或重启用户已有进程。
- 本地日志位于忽略目录 `.codex`；换机器时不能假设日志还在。仓库内计划保留结论，完整远端以 Actions 对应 SHA 的产物为准。
- 达到预算线时停止本任务开发与子代理，记录未提交 / 未验证状态；不得把未完成目标标成 complete 或为了暂停伪造 blocked。
