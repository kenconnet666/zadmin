# ZUI无人值守生产化换机交接

日期：2026-09-02  
仓库：`https://github.com/kenconnet666/zadmin.git`  
分支：`master`  
目标目录（旧电脑）：`C:\code\zadmin`

## 1. Git检查点

换机前已推送的上一检查点：

- `b0692db` `feat(zui): productionize foundation display and feedback primitives`
- `b1bfdc6` `docs(zui): expand production references and resilient routing`
- `fa69e33` `test(zui): lock production contracts and browser ownership`

本交接文件与最新Overlay、PinInput/InputGroup、Tag/Table、CommandPalette、Box/Icon/Stack、Carousel改动会在同一轮后续提交推送。新电脑以`origin/master`最新HEAD为准，不依赖旧电脑未提交文件。

## 2. 新电脑最短恢复路径

```powershell
git clone https://github.com/kenconnet666/zadmin.git
Set-Location .\zadmin
git checkout master
git pull --ff-only origin master
corepack enable
pnpm install --frozen-lockfile
```

然后用WebStorm打开仓库根目录：

1. 在`apps/docs/package.json`运行`dev`脚本；旧电脑的运行配置显示名为`dev (2)`，但该显示名可能是IDE本地状态，不应作为可移植依赖。
2. 文档站默认地址为`http://localhost:5174/`。
3. 若需要命令行兜底：`pnpm --filter @zadmin/docs dev`。
4. 不复制任何旧电脑凭据、Codex配置或缓存进仓库。

## 3. 当前实现状态

已建立并持续收口：

- 141项公开组件metadata/API合同；
- 79个组件文档页、400个差异化Demo、106个literal Docs E2E引用；
- 六主题、typed locale、ICSS、SSR/CSP/HMR、Portal/Layer/Presence、Form/Field、Collection/Virtualizer；
- Button/Toggle/Code、List/DescriptionList、Empty/Result、Progress/Meter/Skeleton、Statistic/Timeline、Alert/Spinner/LoadingBar；
- Dialog/AlertDialog/Popover生产焦点、ARIA、异步决策和owner-realm生命周期；
- PinInput/InputGroup的Field/FormValueBridge、nullable clear、IME/grapheme、外部form、唯一control owner；
- Tag typed locale、size/tone/remove owner；Table原生语义与真实overflow scroll owner；
- CommandPalette 5个source-backed场景；Box/Icon/Stack各4个场景；Carousel 5个场景；
- browser direct mount统一tracked lifecycle与CI静态门禁；
- Docs hash router由main入口rune state持有，跨组件HMR继续导航。

## 4. 换机前快速验证边界

本轮最新统一结果：

- component API generator成功；
- 141项API contract生成与检查成功；
- system audit成功：145个组件文件、141个metadata、79页、400个Demo、0 raw Docs交互控件、0资源生命周期违规、0危险DOM sink；
- browser lifecycle audit成功；
- 各并行批次的核心、文档和专属测试源码均由WebStorm `errorsOnly`检查为0；
- Prettier与`git diff --check`由各批次完成；
- 未运行本地Vitest、Playwright、svelte-check、全量TypeScript、build、coverage或bundle门禁，符合既定工作流。

上一批`fa69e33`曾完成真实Chrome 20页、窄视口和关键交互验收；本交接轮新增的Overlay、PinInput/InputGroup、Tag/Table及补充文档没有再次做完整真实Chrome验收，必须在新电脑或CI复核，不能沿用上一批结论。

## 5. 最近一次CI事实

已按“下一次推送前只查看上一轮一次”检查：

- [CI run 33572547759](https://github.com/kenconnet666/zadmin/actions/runs/33572547759)
- 对应：`fa69e33`
- Workspace builds：成功
- Static contracts：类型/Svelte步骤失败
- Workspace component tests：失败
- Docs E2E：失败
- ZUI coverage：失败；其他coverage子项目成功
- ZUI/SvelteKit SSR外部验收：失败；bundle检查、publish dry-run、WebView/Miniapp外部包成功
- Windows C# core build/test：成功；desktop TypeScript/Svelte失败

本交接推送后的新CI不要等待。新电脑继续编码；只在再下一次正常推送前查看这次交接HEAD对应运行，并处理明确失败。

## 6. 新电脑继续顺序

1. `git status --short`确认干净，记录`git log -5 --oneline`。
2. WebStorm运行Docs，真实Chrome定向检查：Dialog、AlertDialog、Popover、PinInput、InputGroup、Tag、Table、CommandPalette。
3. 查看交接HEAD的上一轮CI一次，优先修Static contracts，再修workspace tests、Docs E2E、SSR package与Windows desktop类型失败。
4. 完成剩余低示例组件：Checkbox、Field、Slider、Command；每页至少4个不同场景，然后把文档最低Demo门禁从2提高到4。
5. 再审计候选新组件，只在真实后台场景需要时新增；优先Breadcrumb、ScrollArea、CheckboxGroup，不同时铺开全部候选。
6. 每个可构建阶段拆分清晰commit并推送`origin/master`；永不等待当前CI。

## 7. 必须保持的工作约束

- 本地只做WebStorm error级、快速generator/audit与真实Chrome定向检查；慢验证交CI。
- 不运行或等待本地全量测试/build/coverage/bundle。
- 不等待当前CI；下一次推送前只检查上一轮。
- Docs优先dogfood ZUI；图标使用`@lucide/svelte/icons/*`，除ZProgress数据圆弧和品牌资源外不手画SVG。
- 保持native API → existing dependency → thin adapter → custom runtime优先级。
- 不用降低阈值、retry、跳过WebKit或删除断言伪造绿色。
- 保持工作树用户改动；禁止`git reset --hard`、广泛restore或删除共享缓存。

## 8. 关键文件

- `.docs/zui/production-completion-blueprint.md`
- `.docs/zui/production-audit.md`
- `.docs/zui/HANDOFF-2026-09-02.md`
- `apps/docs/scripts/audit-system.mjs`
- `apps/docs/tests/site.e2e.ts`
- `ui/zui/src/entrypoints/index.ts`
- `ui/zui/src/runtime/foundation/locale.ts`
- `.github/workflows/ci.yml`

若新电脑没有Svelte MCP/list-sections/svelte-autofixer，继续如实记录并使用WebStorm Svelte/TypeScript error级诊断；不要声称调用不存在的工具。
