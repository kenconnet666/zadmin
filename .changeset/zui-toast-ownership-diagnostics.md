---
'@zadmin/zui': minor
---

Add ToastQueue ownership diagnostics and optional debug names, document caller-owned queue replacement, and make dispose an idempotent terminal boundary whose late tasks cannot recreate records.
