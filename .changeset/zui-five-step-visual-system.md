---
'@zadmin/zui': minor
---

Unify five-step control sizing, independent indicator and surface scales, eight typography sizes, and status color roles across component families. Separate button appearance (`solid`, `outline`, `ghost`) from semantic tone, add explicit typography and trend controls, and align panel and collection sizing.

Rename TreeSelect virtualization props to `virtual`, `virtualHeight`, `virtualItemSize`, and `virtualOverscan`; reject child content on controls that cannot render it. Tighten recipe default and compound selections, improve responsive layout and motion lifecycle handling, and expand visual/API regression coverage.
