---
'@zadmin/zui': patch
---

Restore canonical date-field drafts when a model rejects a picker edit, and preserve nested ToggleGroup focus registration when Toolbar layout changes without changing ownership.

Keep Calendar and date-time generic props precise without expanding native DOM attributes across every semantic branch. Preserve local/zoned, selection-mode and inline/popover constraints in source and consumer types.

Correct the shared drag geometry timing used by keyboard Sortable targets and normalize theme animation durations. Restore adjacent FormList field focus after WebKit removes the focused row without stealing external or vacant document focus during programmatic removal.
