---
'@zadmin/zui': minor
---

Add recursive structured-member metadata and path-aware lifecycle documentation for nested public data shapes. List, DescriptionList, and Timeline now expose complete item fields and scoped `id` to `key` migration facts without confusing conditional identity branches with unconditional required fields.
