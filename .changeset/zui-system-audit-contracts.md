---
'@zadmin/zui': minor
---

Unify Field state and size propagation, correct disabled rendering, and add shared entry motion to overlays and disclosure content. Add complete status color roles and typed ICSS animation/wrapping keywords.

Rename Tooltip and TooltipGroup `delay` to `openDelay`, Tour dismissal flags to `dismissOnEscape` and `dismissOnMaskClick`, and DateField/TimeField reset callbacks to `onFormReset`. Replace the ambiguous ICSS `display.hidden` alias with `display.none`.
